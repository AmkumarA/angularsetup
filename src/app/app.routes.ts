import { Routes } from '@angular/router';
import { UseDetailsComponent } from '../components/use-details/use-details.component';

export const routes: Routes = [
    { path: '', component: UseDetailsComponent }
];
